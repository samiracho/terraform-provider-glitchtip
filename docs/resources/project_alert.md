---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "glitchtip_project_alert Resource - glitchtip"
subcategory: ""
description: |-
  Manages a GlitchTip project alert (notification rule). An alert fires when a project exceeds a threshold of events within a timespan, or on uptime monitor failures, and notifies the configured recipients. Note: the zulip recipient type is not supported in v1 because it requires additional secret fields that the API does not return.
---

# glitchtip_project_alert (Resource)

Manages a GlitchTip project alert (notification rule). An alert fires when a project exceeds a threshold of events within a timespan, or on uptime monitor failures, and notifies the configured recipients. Note: the `zulip` recipient type is not supported in v1 because it requires additional secret fields that the API does not return.

## Example Usage

```terraform
resource "glitchtip_project_alert" "example" {
  organization     = glitchtip_organization.example.slug
  project          = glitchtip_project.example.slug
  name             = "High error rate"
  timespan_minutes = 10
  quantity         = 100

  alert_recipients = [
    {
      recipient_type = "email"
    },
    {
      recipient_type = "webhook"
      url            = "https://hooks.example.com/glitchtip"
    },
  ]
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `organization` (String) Slug of the organization that owns the project. Changing this forces a new alert to be created.
- `project` (String) Slug of the project the alert belongs to. Changing this forces a new alert to be created.

### Optional

- `alert_recipients` (Attributes List) Recipients notified when the alert fires. (see [below for nested schema](#nestedatt--alert_recipients))
- `name` (String) Human-readable name of the alert.
- `quantity` (Number) Number of events within `timespan_minutes` that triggers the alert.
- `timespan_minutes` (Number) Window, in minutes, over which `quantity` events are counted before the alert fires.
- `uptime` (Boolean) When true, the alert is sent on any uptime monitor check failure. Defaults to `false`.

### Read-Only

- `id` (Number) Numeric identifier of the alert.

<a id="nestedatt--alert_recipients"></a>
### Nested Schema for `alert_recipients`

Required:

- `recipient_type` (String) Type of recipient. One of `email`, `discord`, `webhook`, `googlechat`, `teams`, `ntfy`, `feishu`. The `zulip` type is not supported in v1.

Optional:

- `tags_to_add` (List of String) Optional list of additional tags to include in the alert notification.
- `url` (String) Webhook URL the notification is delivered to. For `email` this may be omitted or empty; for the webhook-style types (`discord`, `webhook`, `googlechat`, `teams`, `ntfy`, `feishu`) a valid URL is required.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# Project alerts are imported as "<organization-slug>/<project-slug>/<alert-id>".
terraform import glitchtip_project_alert.example acme-inc/api/42
```
